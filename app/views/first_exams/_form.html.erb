<%= form_for(first_exam) do |f| %>
  <% if first_exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(first_exam.errors.count, "error") %> prohibited this first_exam from being saved:</h2>

      <ul>
      <% first_exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_exam_id %>
    <%= f.number_field :first_exam_id %>
  </div>

  <div class="field">
    <%= f.label :grade %>
    <%= f.text_field :grade %>
  </div>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.number_field :student_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
